test_format = 1.0

# Alby Hub first run has not completed wallet setup, so the endpoint serves
# the app shell (not a completed setup page). The curl test below just checks
# that the proxied root is reachable and returns HTTP 200.

[default]

    [default.curl_tests]
    home.path = "/"
    home.expect_return_code = 200
